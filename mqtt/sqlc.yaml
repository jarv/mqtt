version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "schema/schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
        emit_db_tags: true
