{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "mise run build-release ${nextRelease.version}",
        "publishCmd": "echo 'Version ${nextRelease.version} published'"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          { "path": "mqtt/mqtt-linux-amd64", "label": "mqtt-linux-amd64" },
          { "path": "mqtt/mqtt-linux-arm64", "label": "mqtt-linux-arm64" }
        ]
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["CHANGELOG.md"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ]
  ]
}
