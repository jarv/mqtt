{
  "name": "mqtt-tracker",
  "description": "MQTT device tracker",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:prod": "NODE_ENV=production node build.js"
  },
  "dependencies": {
    "reconnecting-websocket": "^4.4.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@tailwindcss/cli": "^4.0.0",
    "esbuild": "^0.24.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "prettier": "^3.0.0",
    "tailwindcss": "^4.0.0"
  }
}
